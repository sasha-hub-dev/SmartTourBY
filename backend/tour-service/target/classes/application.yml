server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/smarttour_db
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update # Автоматически создаст таблицу
    show-sql: true